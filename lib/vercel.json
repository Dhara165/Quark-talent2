{
  "buildCommand": "cd artifacts/hr-consulting && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/hr-consulting/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}