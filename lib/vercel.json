{
  "buildCommand": "cd artifacts/hr-consulting && npm install && npm run build",
  "outputDirectory": "artifacts/hr-consulting/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}